 <div id="sidebar" class="span4 bluebox">
	<div class="sidebar tanbox" id="search">
	  <%= form_tag search_path, :method => 'get' do %>
	  <%= text_field_tag :search, params[:search] %>
	  <%= submit_tag "Search", :name => nil, :style => "margin-bottom:9px" %>
	<% end %>
	</div>
	<div class="sidebar tanbox" id="categories">
	<h3>Categories:</h3>
	<% Category.all.each do |category| %>
		<%= link_to category.name.titleize, search_category_path(category) %><br/>
	<% end %>
	</div>
	<div class="sidebar tanbox" id="search">
	  <p>Subsribe via e-mail and receive notifications of new posts!</p>
	  <%= form_tag subscribe_init_path, { :method => 'post', :remote => true }  do %>
	  <%= text_field_tag :subscribe, params[:subscribe] %>
	  <%= submit_tag "Subscribe", :name => nil, :style => "margin-bottom:9px" %>
	<% end %>
	</div>
</div>